(in-package :http-routes.test)

(defsuite* (matcher :in http-routes-tests))

(deftest http-routes-tests/matcher/add-pattern-test ()
  (let ((bag (create-patterns-bag)))
    )
  )

(deftest http-routes-tests/matcher/simple ()
  (let (()))
  )

(deftest http-routes-tests/matcher/simple-wildcard ())

(deftest http-routes-tests/matcher/in-the-middle-wildcard ())

(deftest http-routes-tests/matcher/wildcards ())
